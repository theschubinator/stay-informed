  <%= f.label :name %><br>
  <%= f.text_field :name, size: "40" %><br><br>

  Categories: <%= f.collection_check_boxes :category_ids, current_user.categories.uniq, :id, :name %><br><br>
  
  <p>New Category 1:
  <%= f.fields_for :categories, @task.categories.build do |category| %>
    <%= category.text_field :name %>
  <% end %><br><br></p>

   <p>New Category 2:
    <%= f.fields_for :categories, @task.categories.build do |category| %>
    <%= category.text_field :name %>
  <% end %><br><br></p>

  <%= f.label :description %><br>
  <%= f.text_area :description, size: "40x10" %><br><br>

  <%= f.label :due_date %>:
  <%= f.datetime_select :due_date, ampm: true, default: 5.days.from_now %> <br><br>

  <%= f.submit %>