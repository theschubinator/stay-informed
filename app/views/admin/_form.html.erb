  <%= f.label :name %>
  <%= f.text_field :name, size: "53" %><br><br>

  <p>Select Users
  <%= f.collection_check_boxes :user_id, User.all, :id, :email %><br><br></p>

  Categories: <%= f.collection_check_boxes :category_ids, current_user.categories.uniq, :id, :name %><br><br>
  
  <p>New Category 1:
  <%= f.fields_for :categories, @task.categories.build do |category| %>
    <%= category.text_field :name, size: "60" %>
  <% end %><br><br></p>

   <p>New Category 2:
    <%= f.fields_for :categories, @task.categories.build do |category| %>
    <%= category.text_field :name, size: "60" %>
  <% end %><br><br></p>

  <%= f.label :description %><br>
  <%= f.text_area :description, size: "60x2" %><br><br>

  <%= f.label :due_date %>:
  <%= f.datetime_select :due_date, ampm: true, default: 1.hour.from_now %><br><br>

  <%= f.hidden_field :grouped, value: true %>

  <%= f.submit %>